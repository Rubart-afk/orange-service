{
  "name": "default-project",
  "version": "1.0.0",
  "description": "Отклик — база знаний и рабочее пространство для B2B-коммуникаций",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check store.js && node --check app.js && node --check public/app.js",
    "test": "node --test",
    "generate:secrets": "node scripts/generate-secrets.js",
    "backup": "node scripts/backup.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "express": "^5.2.1",
    "google-auth-library": "^11.0.2"
  }
}
